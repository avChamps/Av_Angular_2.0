import { LinkifyPipePipe } from './linkify-pipe.pipe';

describe('LinkifyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LinkifyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
